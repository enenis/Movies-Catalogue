import { StyleSheet,Dimensions } from 'react-native'

export default StyleSheet.create({
    container:{
        flex:1,
        flexDirection:"row",
        margin: 10,
    },
    image:{
        width:70,
        height:70,
        borderRadius:50,
        borderWidth:1,
        borderColor:"orange"
    }
})